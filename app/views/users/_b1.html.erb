<h1>User info</h1>

<%= flash[:success] %>

  <table class="table table-striped">
<tr><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no_img.jpg") %></tr>

        <tr>
  		<tr>
  			<td>name</td>
  			<td><%= user.name %></td>
  		</tr>
  		<tr>
  			<td>introduction</td>
  			<td><%= user.introduction%></td>
  		</tr>
  </table>
  <%= link_to "" ,edit_user_path(user),class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>

   <h2>New book</h2>
   <%= form_for(@book) do |f| %>

	<h4>Title</h4>
    <%= f.text_field :title %>

	<h4>Opinion</h4>
	<%= f.text_area :body %>

	<%= f.submit 'Create Book', class: "btn btn-primary" %>

<% end %>